TODO: Decide whether Spock's context_brief should be a single prose block or lightly structured (e.g., "present: ...", "holding: ...", "recent: ..."). Content is settled — relevant chars/locs with names + one-line summaries, inventory highlights, current direction, recent commitments. Try prose first; fall back to light structure if Tolkien starts drifting.

TODO: Refine the data/test_scenario.json. Should be properly written so that it accuractely can test the two worklows. Should for example be much longer.

TODO: Replace the custom JSON + Markdown logging (`src/util/interaction_logger.py`, `src/util/story_log.py`) with a LangGraph-native inspection path. The obvious "inspection tool" answer is LangGraph Studio (`langgraph dev`), but it's primarily a live-dev UI and needs a free LangSmith account even when the graph runs locally — not the right archival fit. The better candidate is to attach a `SqliteSaver` checkpointer to the compiled graph (per-session `thread_id`), then walk `graph.get_state_history(config)` offline to step through every super-step's state. Gives time-travel replay for free and deletes a custom code path. Caveat: checkpointers persist state snapshots only, not raw prompts / token usage / latency. If those still need to be archival artifacts, either fold them into `StoryState` or persist `graph.astream_events(...)` to JSONL alongside. The Markdown transcript only stays if a human-skimmable narrative artifact is still wanted after the rewrite.

TODO: Re-evaluate `wan2.6-i2v-flash` vs `wan2.2-i2v` for the live demo. 2.6's only real win is per-clip variable duration (Spielberg picks 5/10s); in practice it loses on the things that actually matter for this story — the studio-white background drifts across clips, and end-frame → next-clip continuity is visibly worse than 2.2. Decide: switch back to 2.2 with fixed-duration clips (and either drop `Shot.duration_seconds` or keep it ignored), or stay on 2.6 and frame the variable-duration win as a knowing trade-off. Run a short A/B on the same scenario before committing.

TODO: Install the Langfuse AI skill from github.com/langfuse/skills and use it to add tracing to this application with Langfuse following best practices.
