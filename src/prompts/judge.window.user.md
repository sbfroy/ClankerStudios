# Probe: {probe_name}

**Configuration:** {config_name}
**Window:** turns {window_start} → {score_at_turn}
**Probe focus:** {probe_asks}

{annotation_block}

## Transcript

{transcript}

Score the five memory dimensions described in the system prompt, focusing on the probe at turn {score_at_turn} but reading the whole window for context. Return JSON only.
